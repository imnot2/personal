/**
 * Composition interface.
 *
 * Represents any constructor or instance returned by any {@link compose.factory} method.
 * @class compose.composition
 * @interface
 * @private
 */

/**
 * Extend this constructor from multiple others constructors/objects.
 * @method extend
 * @inheritable
 * @static
 * @param {...(Function|Object)} composition One or more constructors or objects to be mixed in.
 * @return {compose.composition} The extended subclass
 */

/**
 * @method create
 * @inheritdoc compose.factory#create
 * @inheritable
 * @static
 */
